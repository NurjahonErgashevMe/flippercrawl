# Скопируйте в proxies.txt (рядом в apps/api) и подставьте свои строки из кабинета провайдера прокси.
# Формат: hostname:port:login:password  или  hostname:port  (логин/пароль тогда в .env)
#
# gw.example.com:823:LOGIN:PASSWORD
# gw.example.com:824:LOGIN:PASSWORD
